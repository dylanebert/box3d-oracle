#!/usr/bin/env bun
import { createHash } from "node:crypto";
import { readFileSync, writeFileSync } from "node:fs";
type Command = Record<string, unknown>;
type Scenario = { id: string; name: string; commands: Command[]; stepCount: number; requiredJointIds?: string[]; requiredSensorEventIds?: string[] };
type Corpus = { schema?: string; corpusVersion?: number; scenarios?: Scenario[] };
const [input, output] = Bun.argv.slice(2);
if (!input || !output) throw new Error("usage: compile-scenarios.ts <commands-v1.json> <temporary-header.h>");
const bytes = readFileSync(input); const corpus = JSON.parse(bytes.toString()) as Corpus;
if (corpus.schema !== "box3d-oracle/scenario-command/v1" || corpus.corpusVersion !== 1 || !Array.isArray(corpus.scenarios)) throw new Error("invalid scenario command corpus");
const opCode: Record<string, number> = { "world.create": 1, "body.create": 2, "body.spawn": 22, "body.apply-mass": 23, "body.set-velocity": 24, "body.target-transform": 25, "resource.box": 3, "resource.sphere": 4, "resource.capsule": 5, "resource.hull": 26, "resource.mesh": 6, "resource.height-field": 7, "resource.compound": 8, "shape.create": 9, "sensor.events": 10, "joint.filter": 27, "joint.revolute": 11, "joint.weld": 12, "joint.parallel": 13, "joint.motor": 14, "joint.distance": 15, "joint.prismatic": 16, "joint.spherical": 17, "joint.wheel": 18, step: 19, observe: 20, hash: 21 };
const jointKinds: Record<string, number> = { revolute: 1, weld: 2, parallel: 3, motor: 4, distance: 5, prismatic: 6, spherical: 7, wheel: 8 };
const jointFields = ["targetAngle", "hertz", "dampingRatio", "lowerAngle", "upperAngle", "maxMotorTorque", "motorSpeed", "maxTorque", "maxVelocityForce", "maxVelocityTorque", "linearHertz", "linearDampingRatio", "maxSpringForce", "angularHertz", "angularDampingRatio", "maxSpringTorque", "length", "minLength", "maxLength", "maxMotorForce", "targetTranslation", "lowerTranslation", "upperTranslation", "coneAngle", "lowerTwistAngle", "upperTwistAngle", "maxSteeringTorque", "targetSteeringAngle", "lowerSteeringLimit", "upperSteeringLimit", "maxSpinTorque", "spinSpeed", "suspensionHertz", "suspensionDampingRatio", "lowerSuspensionLimit", "upperSuspensionLimit", "steeringHertz", "steeringDampingRatio", "linearVelocity", "angularVelocity", "motorVelocity"];
const boolFields = ["enableSpring", "enableLimit", "enableMotor", "enableConeLimit", "enableTwistLimit", "enableSuspensionSpring", "enableSuspensionLimit", "enableSpinMotor", "enableSteering", "enableSteeringLimit"];
const fieldBit = (name: string): string => { const i = jointFields.indexOf(name); if (i < 0) throw new Error(`unknown joint field ${name}`); return `UINT64_C(1) << ${i}`; };
const boolBit = (name: string): string => { const i = boolFields.indexOf(name); if (i < 0) throw new Error(`unknown joint boolean field ${name}`); return `UINT64_C(1) << ${48 + i}`; };
const f32 = (value: unknown): string => { if (typeof value !== "string" || !/^0x[0-9a-f]{8}$/.test(value)) throw new Error(`f32 input is not fixed-width hex: ${String(value)}`); return value; };
const words = (values: unknown[], count: number): string[] => { if (!Array.isArray(values) || values.length !== count) throw new Error(`expected ${count} f32 words`); return values.map(f32); };
const ints = (values: unknown[], count: number): number[] => { if (!Array.isArray(values) || values.length !== count || values.some((x) => !Number.isInteger(x) || (x as number) < 0)) throw new Error(`expected ${count} non-negative integer values`); return values as number[]; };
const cString = (value: string): string => JSON.stringify(value);
const numberId = (value: unknown, prefix: string, scene: string): number => { const match = new RegExp(`^${prefix}([0-9]+)$`).exec(String(value)); if (!match) throw new Error(`invalid ${prefix} id ${String(value)} in ${scene}`); return Number(match[1]); };
const arrays: string[] = []; const records: string[] = []; const names = new Set<string>();
const material = (value: unknown): string => { const m = (value ?? {}) as Record<string, unknown>; const tangent = words((m.tangentVelocity ?? ["0x00000000", "0x00000000", "0x00000000"]) as unknown[], 3); return `{ ${f32(m.friction ?? "0x3f19999a")}, ${f32(m.restitution ?? "0x00000000")}, ${f32(m.rollingResistance ?? "0x00000000")}, { ${tangent.join(", ")} }, UINT64_C(${String(m.userMaterialId ?? "0x0000000000000000").replace(/^0x/, "0x")}), ${Number(m.customColor ?? 0)} }`; };
const transform = (value: unknown): string[] => { const x = (value ?? { p: ["0x00000000", "0x00000000", "0x00000000"], q: ["0x00000000", "0x00000000", "0x00000000", "0x3f800000"] }) as Record<string, unknown>; return [...words(x.p as unknown[], 3), ...words(x.q as unknown[], 4)]; };
for (let si = 0; si < corpus.scenarios.length; si++) {
  const scenario = corpus.scenarios[si];
  if (!scenario.id || !scenario.name || !Array.isArray(scenario.commands) || !Number.isInteger(scenario.stepCount)) throw new Error(`invalid scenario at ${si}`);
  if (names.has(scenario.name) || names.has(scenario.id)) throw new Error(`duplicate scenario ${scenario.name}`); names.add(scenario.name); names.add(scenario.id);
  const ids = new Set<string>(); const definedBodies = new Set<number>(); const definedResources = new Set<number>(); const resourceKinds = new Map<number, string>(); const compoundPointers = new Map<number, string>(); const definedShapes = new Set<number>(); const definedJoints = new Set<string>(); const rows: string[] = [];
  for (let ci = 0; ci < scenario.commands.length; ci++) {
    const command = scenario.commands[ci]; const op = String(command.op ?? ""); if (!(op in opCode)) throw new Error(`unknown command op ${op}`);
    const id = String(command.id ?? ""); if (!id || ids.has(id)) throw new Error(`duplicate or missing command id ${id} in ${scenario.name}`); ids.add(id);
    const values = new Array<string>(80).fill("0x00000000"); const bodies = new Array<number>(512).fill(-1); let a = -1, b = -1, kind = 0, step = -1, bodyCount = 0, substeps = 0; const capacity = [0, 0, 0, 0, 0]; let flags = "UINT64_C(0)"; let meshPointer = "NULL", heightPointer = "NULL", hullPointer = "NULL", compoundPointer = "NULL";
    if (op === "world.create") { words(command.gravity as unknown[], 3).forEach((x, i) => values[i] = x); const cap = command.capacity as Record<string, unknown> | undefined; if (cap) ["staticShapeCount", "dynamicShapeCount", "staticBodyCount", "dynamicBodyCount", "contactCount"].forEach((key, i) => { if (!Number.isInteger(cap[key])) throw new Error(`invalid world capacity in ${scenario.name}`); capacity[i] = Number(cap[key]); }); if (command.enableSleep === true) flags += " | UINT64_C(1) << 62"; if (command.enableContinuous === true) flags += " | UINT64_C(1) << 63"; }
    else if (op === "body.create" || op === "body.spawn") { a = numberId(id, "b", scenario.name); if (definedBodies.has(a)) throw new Error(`duplicate body ${id}`); definedBodies.add(a); kind = command.type === "static" ? 0 : command.type === "kinematic" ? 1 : command.type === "dynamic" ? 2 : -1; if (kind < 0) throw new Error(`unknown body type in ${scenario.name}`); words(command.position as unknown[], 3).forEach((x, i) => values[i] = x); words((command.rotation ?? ["0x00000000", "0x00000000", "0x00000000", "0x3f800000"]) as unknown[], 4).forEach((x, i) => values[3 + i] = x); words(command.linearVelocity as unknown[], 3).forEach((x, i) => values[7 + i] = x); words(command.angularVelocity as unknown[], 3).forEach((x, i) => values[10 + i] = x); values[13] = f32(command.linearDamping ?? "0x00000000"); values[14] = f32(command.angularDamping ?? "0x00000000"); values[15] = f32(command.sleepThreshold ?? "0x3d4ccccd"); if (command.isBullet === true) flags += " | UINT64_C(1) << 61"; if (op === "body.spawn") step = Number(command.step); }
    else if (op === "body.apply-mass" || op === "body.set-velocity" || op === "body.target-transform") { a = numberId(command.body, "b", scenario.name); if (!definedBodies.has(a)) throw new Error(`body action references unknown body in ${scenario.name}`); step = command.step === undefined ? -1 : Number(command.step); if (op === "body.set-velocity") { words(command.linearVelocity as unknown[], 3).forEach((x, i) => values[i] = x); words(command.angularVelocity as unknown[], 3).forEach((x, i) => values[3 + i] = x); } else if (op === "body.target-transform") { const x = transform(command.target); x.forEach((word, i) => values[i] = word); values[7] = f32(command.timeStep); if (command.wake === true) flags += " | UINT64_C(1) << 56"; } }
    else if (["resource.box", "resource.sphere", "resource.capsule", "resource.hull", "resource.mesh", "resource.height-field"].includes(op)) {
      a = numberId(id, "r", scenario.name); if (definedResources.has(a)) throw new Error(`duplicate resource ${id}`); definedResources.add(a); resourceKinds.set(a, op);
      if (op === "resource.box") { kind = 1; words(command.halfExtents as unknown[], 3).forEach((x, i) => values[i] = x); words((command.center ?? ["0x00000000", "0x00000000", "0x00000000"]) as unknown[], 3).forEach((x, i) => values[3 + i] = x); }
      else if (op === "resource.sphere") { kind = 2; words((command.center ?? ["0x00000000", "0x00000000", "0x00000000"]) as unknown[], 3).forEach((x, i) => values[i] = x); values[3] = f32(command.radius); }
      else if (op === "resource.capsule") { kind = 3; words(command.center1 as unknown[], 3).forEach((x, i) => values[i] = x); words(command.center2 as unknown[], 3).forEach((x, i) => values[3 + i] = x); values[6] = f32(command.radius); }
      else if (op === "resource.hull") { kind = 7; const points = command.points as unknown[]; if (!Array.isArray(points) || points.length < 4) throw new Error(`hull point count mismatch in ${scenario.name}`); const name = `scenario_hull_${si}_${ci}`; const flat = points.flatMap((point) => { if (!Array.isArray(point)) throw new Error(`hull point is not a vector in ${scenario.name}`); return words(point, 3); }); arrays.push(`static const uint32_t ${name}_points[] = { ${flat.join(", ")} };`, `static const ScenarioHullData ${name} = { ${name}_points, ${points.length} };`); hullPointer = `&${name}`; }
      else if (op === "resource.mesh") { kind = 4; const vertices = command.vertices as unknown[]; const indices = ints(command.indices as unknown[], Number(command.triangleCount) * 3); if (!Array.isArray(vertices) || vertices.length !== Number(command.vertexCount) * 3) throw new Error(`mesh vertex count mismatch in ${scenario.name}`); const name = `scenario_mesh_${si}_${ci}`; arrays.push(`static const uint32_t ${name}_vertices[] = { ${words(vertices, vertices.length).join(", ")} };`, `static const int32_t ${name}_indices[] = { ${indices.join(", ")} };`, `static const ScenarioMeshData ${name} = { ${name}_vertices, ${Number(command.vertexCount)}, ${name}_indices, ${Number(command.triangleCount)}, ${command.useMedianSplit === true ? 1 : 0}, ${command.identifyEdges === true ? 1 : 0} };`); meshPointer = `&${name}`; }
      else { kind = 5; const samples = command.samples as unknown[]; const countX = Number(command.countX); const countZ = Number(command.countZ); const cellCount = (countX - 1) * (countZ - 1); if (!Array.isArray(samples) || samples.length !== countX * countZ) throw new Error(`height-field sample count mismatch in ${scenario.name}`); const materials = command.materialIndices === undefined ? new Array(cellCount).fill(0) : ints(command.materialIndices as unknown[], cellCount); const name = `scenario_height_${si}_${ci}`; arrays.push(`static const uint32_t ${name}_samples[] = { ${words(samples, samples.length).join(", ")} };`, `static const uint8_t ${name}_materials[] = { ${materials.join(", ")} };`, `static const ScenarioHeightFieldData ${name} = { ${name}_samples, ${name}_materials, ${countX}, ${countZ}, ${f32(command.scaleX)}, ${f32(command.scaleY)}, ${f32(command.scaleZ)}, ${f32(command.globalMinimumHeight)}, ${f32(command.globalMaximumHeight)}, ${command.clockwiseWinding === true ? 1 : 0} };`); heightPointer = `&${name}`; }
    } else if (op === "resource.compound") {
      a = numberId(id, "r", scenario.name); if (definedResources.has(a)) throw new Error(`duplicate resource ${id}`); definedResources.add(a); resourceKinds.set(a, op); const defs: string[] = []; const childIds: string[] = [];
      for (const [field, childKind] of [["capsules", "resource.capsule"], ["hulls", "resource.box"], ["meshes", "resource.mesh"], ["spheres", "resource.sphere"]] as const) {
        const children = (command[field] ?? []) as unknown[]; if (!Array.isArray(children)) throw new Error(`compound ${id} ${field} is not an array`);
        const cname = `scenario_compound_${si}_${ci}_${field}`; const childRows = children.map((raw) => { const child = raw as Record<string, unknown>; const childId = String(child.id ?? ""); if (!childId || childIds.includes(childId)) throw new Error(`compound child IDs are missing or duplicated in ${scenario.name}`); childIds.push(childId); const resource = numberId(child.resource, "r", scenario.name); if (!definedResources.has(resource) || resourceKinds.get(resource) !== childKind) throw new Error(`compound child resource reference is unresolved or wrong kind in ${scenario.name}`); const x = transform(child.transform); const scale = words((child.scale ?? ["0x3f800000", "0x3f800000", "0x3f800000"]) as unknown[], 3); const mats = (child.materials ?? [child.material]) as unknown[]; if (!Array.isArray(mats) || mats.length < 1 || mats.length > 4) throw new Error(`compound material list is invalid in ${scenario.name}`); return `{ ${resource}, { ${x.join(", ")} }, { ${scale.join(", ")} }, ${mats.length}, { ${mats.map(material).join(", ")} } }`; });
        arrays.push(`static const ScenarioCompoundChild ${cname}[] = { ${childRows.join(", ")} };`); defs.push(`${cname}, ${children.length}`);
      }
      const declaredOrder = command.childOrder; if (!Array.isArray(declaredOrder) || JSON.stringify(declaredOrder) !== JSON.stringify(childIds)) throw new Error(`compound child order is not explicit in ${scenario.name}`); const cname = `scenario_compound_${si}_${ci}`; arrays.push(`static const ScenarioCompoundData ${cname} = { ${defs.join(", ")} };`); compoundPointers.set(a, `&${cname}`); compoundPointer = `&${cname}`;
    } else if (op === "shape.create") {
      const shapeMatch = /^s(?:h)?([0-9]+)$/.exec(id); if (!shapeMatch) throw new Error(`invalid shape id ${id} in ${scenario.name}`); a = Number(shapeMatch[1]); if (definedShapes.has(a)) throw new Error(`duplicate shape ${id}`); definedShapes.add(a); b = numberId(command.body, "b", scenario.name); const r = numberId(command.resource, "r", scenario.name); if (!definedBodies.has(b) || !definedResources.has(r)) throw new Error(`shape reference before definition in ${scenario.name}`); kind = command.kind === "box" ? 1 : command.kind === "sphere" ? 2 : command.kind === "capsule" ? 3 : command.kind === "hull" ? 7 : command.kind === "mesh" ? 4 : command.kind === "height-field" ? 5 : command.kind === "compound" ? 6 : -1; if (kind < 0) throw new Error(`unknown shape kind in ${scenario.name}`); if (kind === 6 && resourceKinds.get(r) !== "resource.compound") throw new Error(`compound shape does not reference compound resource in ${scenario.name}`); if (kind === 6) compoundPointer = compoundPointers.get(r) ?? "NULL"; values[0] = `0x${r.toString(16).padStart(8, "0")}`; values[1] = f32(command.rollingResistance ?? "0x00000000"); values[2] = `0x${Number(command.groupIndex ?? 0).toString(16).padStart(8, "0")}`; values[15] = f32(command.density ?? "0x447a0000"); values[16] = f32(command.friction ?? "0x3f19999a"); values[17] = f32(command.restitution ?? "0x00000000"); words((command.scale ?? ["0x3f800000", "0x3f800000", "0x3f800000"]) as unknown[], 3).forEach((x, i) => values[3 + i] = x); if (command.updateBodyMass === false) flags += " | UINT64_C(1) << 57"; if (command.invokeContactCreation === false) flags += " | UINT64_C(1) << 58"; if (command.isSensor === true) flags += " | UINT64_C(1) << 60"; if (command.enableSensorEvents === true) flags += " | UINT64_C(1) << 59"; }
    else if (op === "sensor.events") { a = numberId(command.shape, "s", scenario.name); if (!definedShapes.has(a)) throw new Error(`sensor event references unknown shape in ${scenario.name}`); step = Number(command.step); }
    else if (op === "joint.filter") { a = numberId(command.bodyA, "b", scenario.name); b = numberId(command.bodyB, "b", scenario.name); if (!definedBodies.has(a) || !definedBodies.has(b)) throw new Error(`filter joint ${id} references body before definition in ${scenario.name}`); definedJoints.add(id); }
    else if (op.startsWith("joint.")) { const type = op.slice(6); kind = jointKinds[type]; if (command.normalizeFrames === true) flags += " | UINT64_C(1) << 55"; a = numberId(command.bodyA, "b", scenario.name); b = numberId(command.bodyB, "b", scenario.name); if (!definedBodies.has(a) || !definedBodies.has(b)) throw new Error(`joint ${id} references body before definition in ${scenario.name}`); definedJoints.add(id); const fa = command.localFrameA as Record<string, unknown>; const fb = command.localFrameB as Record<string, unknown>; words(fa.p as unknown[], 3).forEach((x, i) => values[i] = x); words(fa.q as unknown[], 4).forEach((x, i) => values[3 + i] = x); words(fb.p as unknown[], 3).forEach((x, i) => values[7 + i] = x); words(fb.q as unknown[], 4).forEach((x, i) => values[10 + i] = x); for (const [name, value] of Object.entries(command)) { if (["op", "id", "bodyA", "bodyB", "localFrameA", "localFrameB", "normalizeFrames"].includes(name)) continue; if (name === "normalizeFrames") continue; if (typeof value === "boolean") { if (value) flags += ` | ${boolBit(name)}`; } else if (Array.isArray(value)) { const index = ["linearVelocity", "angularVelocity", "motorVelocity"].indexOf(name); if (index < 0) throw new Error(`unknown joint vector ${name}`); words(value, 3).forEach((x, i) => values[56 + index * 3 + i] = x); flags += ` | ${fieldBit(name)}`; } else { values[14 + jointFields.indexOf(name)] = f32(value); flags += ` | ${fieldBit(name)}`; } } }
    else if (op === "step") { step = Number(id.slice(5)); if (!Number.isInteger(step)) throw new Error(`invalid step id ${id}`); values[0] = f32(command.timeStep); substeps = Number(command.subStepCount); if (!Number.isInteger(substeps) || substeps < 1) throw new Error(`invalid substep count in ${scenario.name}`); }
    else if (op === "observe") { step = Number(command.step); if (!Number.isInteger(step)) throw new Error(`invalid observation step in ${scenario.name}`); const list = command.bodies; if (!Array.isArray(list) || list.length > 512) throw new Error(`invalid observation list in ${scenario.name}`); for (const item of list) { const body = numberId(item, "b", scenario.name); if (!definedBodies.has(body)) throw new Error(`observation references body before definition in ${scenario.name}`); bodies[bodyCount++] = body; } }
    else if (op === "hash") step = Number(command.step);
    if (["step", "observe", "hash", "sensor.events", "body.spawn", "body.target-transform"].includes(op) && (step < 0 || step >= scenario.stepCount)) throw new Error(`invalid step in ${scenario.name}`);
    rows.push(`  { ${opCode[op]}, ${cString(id)}, ${a}, ${b}, ${kind}, ${step}, ${bodyCount}, ${substeps}, ${flags}, { ${values.join(", ")} }, { ${bodies.join(", ")} }, { ${capacity.join(", ")} }, ${meshPointer}, ${heightPointer}, ${hullPointer}, ${compoundPointer} },`);
  }
  const required = scenario.requiredJointIds ?? []; if (new Set(required).size !== required.length || definedJoints.size !== required.length || required.some((id) => !definedJoints.has(id))) throw new Error(`required joint reference validation failed in ${scenario.name}`); const requiredEvents = scenario.requiredSensorEventIds ?? []; const eventIds = scenario.commands.filter((command) => command.op === "sensor.events").map((command) => String(command.id)); if (new Set(requiredEvents).size !== requiredEvents.length || JSON.stringify(requiredEvents) !== JSON.stringify(eventIds)) throw new Error(`required sensor event schedule validation failed in ${scenario.name}`);
  arrays.push(`static const ScenarioCommand scenario_commands_${si}[] = {\n${rows.join("\n")}\n};`); records.push(`  { ${cString(scenario.id)}, ${cString(scenario.name)}, scenario_commands_${si}, ${rows.length}, ${required.length} },`);
  if (!scenario.commands.some((c) => c.op === "world.create")) throw new Error(`scenario ${scenario.name} has no world.create`);
  for (let s = 0; s < scenario.stepCount; s++) if (!scenario.commands.some((c) => c.op === "observe" && c.step === s) || !scenario.commands.some((c) => c.op === "hash" && c.step === s)) throw new Error(`incomplete observation schedule at ${scenario.name}:${s}`);
}
const digest = createHash("sha256").update(bytes).digest("hex");
const header = `#ifndef BOX3D_SCENARIO_TABLE_V1_H\n#define BOX3D_SCENARIO_TABLE_V1_H\n#include <stdint.h>\n#include <stddef.h>\n#define SCENARIO_CORPUS_DIGEST "${digest}"\ntypedef struct ScenarioMeshData { const uint32_t* vertices; int vertexCount; const int32_t* indices; int triangleCount; int useMedianSplit; int identifyEdges; } ScenarioMeshData;\ntypedef struct ScenarioHullData { const uint32_t* points; int pointCount; } ScenarioHullData;\ntypedef struct ScenarioHeightFieldData { const uint32_t* samples; const uint8_t* materials; int countX; int countZ; uint32_t scaleX; uint32_t scaleY; uint32_t scaleZ; uint32_t minHeight; uint32_t maxHeight; int clockwiseWinding; } ScenarioHeightFieldData;\ntypedef struct ScenarioMaterialData { uint32_t friction, restitution, rollingResistance, tangentVelocity[3]; uint64_t userMaterialId; uint32_t customColor; } ScenarioMaterialData;\ntypedef struct ScenarioCompoundChild { int resource; uint32_t transform[7]; uint32_t scale[3]; int materialCount; ScenarioMaterialData materials[4]; } ScenarioCompoundChild;\ntypedef struct ScenarioCompoundData { const ScenarioCompoundChild* capsules; int capsuleCount; const ScenarioCompoundChild* hulls; int hullCount; const ScenarioCompoundChild* meshes; int meshCount; const ScenarioCompoundChild* spheres; int sphereCount; } ScenarioCompoundData;\nenum ScenarioOp { SCENARIO_WORLD=1, SCENARIO_BODY=2, SCENARIO_BOX_RESOURCE=3, SCENARIO_SPHERE_RESOURCE=4, SCENARIO_CAPSULE_RESOURCE=5, SCENARIO_MESH_RESOURCE=6, SCENARIO_HEIGHT_RESOURCE=7, SCENARIO_COMPOUND_RESOURCE=8, SCENARIO_SHAPE=9, SCENARIO_SENSOR_EVENTS=10, SCENARIO_REVOLUTE=11, SCENARIO_WELD=12, SCENARIO_PARALLEL=13, SCENARIO_MOTOR=14, SCENARIO_DISTANCE=15, SCENARIO_PRISMATIC=16, SCENARIO_SPHERICAL=17, SCENARIO_WHEEL=18, SCENARIO_STEP=19, SCENARIO_OBSERVE=20, SCENARIO_HASH=21, SCENARIO_SPAWN=22, SCENARIO_APPLY_MASS=23, SCENARIO_SET_VELOCITY=24, SCENARIO_TARGET_TRANSFORM=25, SCENARIO_HULL_RESOURCE=26, SCENARIO_FILTER=27 };\ntypedef struct ScenarioCommand { int op; const char* id; int a; int b; int kind; int step; int bodyCount; int substeps; uint64_t flags; uint32_t values[80]; int bodies[512]; int capacity[5]; const ScenarioMeshData* mesh; const ScenarioHeightFieldData* heightField; const ScenarioHullData* hull; const ScenarioCompoundData* compound; } ScenarioCommand;\ntypedef struct ScenarioRecord { const char* id; const char* name; const ScenarioCommand* commands; int commandCount; int requiredJointCount; } ScenarioRecord;\n${arrays.join("\n")}\nstatic const ScenarioRecord scenario_records[] = {\n${records.join("\n")}\n};\nstatic const int scenario_record_count = ${corpus.scenarios.length};\n#endif\n`;
writeFileSync(output, header); console.log(JSON.stringify({ schema: corpus.schema, digest, scenarios: corpus.scenarios.map((s) => ({ id: s.id, name: s.name, commands: s.commands.length, joints: (s.requiredJointIds ?? []).length })) }));
